#include <stdio.h>

int is_zhishu(int number)
{
    int half_number = number / 2;
    for(int i = 2, i < half_number, i++)
    {
        result1 = number / i;
        result2 = number / 
        if()
    }
}