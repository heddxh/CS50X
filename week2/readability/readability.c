#include <cs50.h>
#include <stdio.h>
#include <ctype.h>

int count_letters(string text);

int main(void)
{
    string text = get_string("Text: ");

}

int count_letters(string text)
{
    int count = 0;
    for (int i = 0, )
}