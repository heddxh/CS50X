#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int h
    do
    {
        h = get_int("Height: ");
    }
    while (n > 1); //ensure a valid input

    for (int i = 1, i < h+1, i++)
    {
        for (int j = 0, )
        printf(" ");
        printf("#");
    }
}